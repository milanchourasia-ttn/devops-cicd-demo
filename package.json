{
  "name": "aws-cicd-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static website deployed to Amazon S3 and CloudFront through GitHub Actions",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
